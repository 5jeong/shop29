<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.toy2.shop29.qna.UserMapper">
    <!-- READ   -->
    <select id="select" parameterType="String" resultType="com.toy2.shop29.qna.domain.UserForQnaDto">
        SELECT
            *
        FROM `User`
        WHERE `user_id` = #{userId}
    </select>
</mapper>